name: Run Dow + WGAT Scans

on:
  workflow_dispatch:
    inputs:
      scan_date:
        description: "Scan as-of date (YYYY-MM-DD). Leave blank for today."
        required: false
        default: ""
  push:
    branches: [ main ]
    paths:
      - "stockdata_15/**"
      - "stockdata_1H/**"
      - "stockdata_D/**"
      - "stockdata_W/**"
      - "stockdata_M/**"
      - "wgat/DOW_JSON.py"
      - "wgat/WGAT_JSON.py"
      - "wgat/requirements.txt"
  schedule:
    - cron: "30 11 * * 1-5"

permissions:
  contents: write

concurrency:
  group: scan-${{ github.ref }}
  cancel-in-progress: true

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r wgat/requirements.txt

      - name: Resolve scan date
        id: date
        shell: bash
        run: |
          if [ -n "${{ github.event.inputs.scan_date }}" ]; then
            echo "value=${{ github.event.inputs.scan_date }}" >> "$GITHUB_OUTPUT"
          else
            echo "value=$(date -u +%Y-%m-%d)" >> "$GITHUB_OUTPUT"
          fi

      - name: Run Dow Theory scan
        working-directory: wgat
        run: python DOW_JSON.py --date "${{ steps.date.outputs.value }}" --out resultdow.json

      - name: Run WGAT scan
        working-directory: wgat
        run: python WGAT_JSON.py --date "${{ steps.date.outputs.value }}" --out resultwgat.json

      - name: Verify result files
        run: |
          test -f wgat/resultdow.json
          test -f wgat/resultwgat.json
          ls -lh wgat/resultdow.json wgat/resultwgat.json

      - name: Commit updated results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add wgat/resultdow.json wgat/resultwgat.json
          if git diff --cached --quiet; then
            echo "No changes in scan results — nothing to commit."
          else
            git commit -m "chore: refresh scan results (${{ steps.date.outputs.value }})"
            git pull --rebase origin main
            git push
          fi
